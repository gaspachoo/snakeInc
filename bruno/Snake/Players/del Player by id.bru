meta {
  name: del Player by id
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/api/v1/players/:id
  body: json
  auth: inherit
}

params:path {
  id: 2
}

settings {
  encodeUrl: true
  timeout: 0
}
