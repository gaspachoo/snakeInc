meta {
  name: {{base_url}}/api/v1/hello_1
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/hello
  body: json
  auth: inherit
}

body:json {
  {
      "name" : "Jean Louis"
  }
}

settings {
  encodeUrl: true
}
